server:
  port: 8085

spring:
  http:
    encoding:
      charset: UTF-8
      force: true
  # Thymeleaf配置
  thymeleaf:
    cache: false
    encoding: UTF-8
    mode: LEGACYHTML5
  cache:
    type: redis

# 开启debug模式
debug: true

